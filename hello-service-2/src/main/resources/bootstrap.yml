spring:
  cloud:
    config:
      profile: dev
      label: master
      name: config
      discovery:
        enabled: true
        service-id: config-service
management:
  endpoints:
    web:
      exposure:
        include: refresh